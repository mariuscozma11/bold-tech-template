import type { NextConfig } from "next";
module.exports = {
  images:{
    remotePatterns:[new URL('https://placeholder.pics/svg/600x600')]
  }
}
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
